class DartBasics {
  static dartBasics(){
    // 1. What is a Variable?
    // variable is a named of storage where user can store data 
  
  // int age = 10;
  // double height = 5.5;
  // num personAge = 39;
  // num personHeight = 5.0;

  // print(age);
  // print(height);
  // print(personAge);
  // print(personHeight);


  // What is a variable? How is it different from a constant?
  // variable is a named of storage where user can store data in constant value fixed but we can change variable value 

  }
  
}